# Python
__pycache__/
*.pyc
.venv/
venv/

# Monarch session cache (the library writes a session token file on login)
.mm/
.monarch*
*.session

# Env / secrets — never commit
.env
.env.*
!.env.local.example

# Real personal spending data — kept local-only, never committed or deployed.
# The shared site uses data/spending.fake.json instead.
data/spending.json

# Logs (from launchd refresh job)
logs/

# OS
.DS_Store

# Node / Playwright (dev + CI only — the app itself is no-build)
node_modules/
/screenshots/
/test-results/
/playwright-report/
.playwright-mcp/
# stray verification screenshots from local runs
/task*-verify.png
/task*.png
/page-*.png

# Session scratch / handoff notes — not part of the project
docs/SESSION-HANDOFF.md
.vercel
